I want to start a production-oriented OCI inference project that combines a pre-existing public statistical / ML model with a vLLM-hosted LLM companion service.

Project objective:
Build a prototype architecture where a traditional ML/statistical model is loaded from an existing public source, such as Hugging Face or another public model repository, and used to produce predictions, risk bands, driver contributions, and structured outputs. A vLLM-served LLM should then generate natural-language explanations, recommendations, and optional structured text-derived features.

Important clarification:
I do not want to train a statistical or ML model in this project. The project should assume we are using an already-trained public model or placeholder public model artifact. Hugging Face is one example source, but any reasonable public statistical, ML, transformer, ONNX, PyTorch, TensorFlow, XGBoost, or scikit-learn-compatible model can be considered if it fits the architecture. The goal is inference-serving and orchestration, not model training.

I do not want you to choose the public model for me immediately. Before creating the final scaffold around a model, give me a curated menu of interesting public model options and let me pick one.

Target environment:
- Oracle Cloud Infrastructure
- Compute target: OCI E6 AX shape
- Primary data store: Oracle Autonomous Database, if appropriate
- Consumption layer may eventually include APEX and/or Oracle Analytics Cloud
- Containerized deployment preferred
- Assume this starts as a prototype but should be designed with a path to production

Important architectural principle:
Do not use vLLM as the serving engine for a classical scikit-learn/statistical model. Instead:
- Serve the traditional ML/statistical model separately.
- Use vLLM for LLM inference, embeddings, feature extraction from text, explanation generation, and conversational or narrative assistance around the ML output.

Please act as a senior OCI ML platform engineer and help me create the initial project scaffold.

Phase 1: Model option discovery — do this first
Before writing project code, give me a curated set of public model options I could use for the non-vLLM inference service.

For each option, include:
- Model name
- Source, such as Hugging Face, ONNX Model Zoo, scikit-learn example artifact, XGBoost example artifact, TensorFlow Hub, PyTorch Hub, or another public source
- Model/task type
- Why it is interesting
- What input it expects
- What output it produces
- Whether it is a classical ML/statistical model, transformer model, embedding model, computer vision model, tabular model, time-series model, anomaly detection model, or another category
- How easy it is to serve with FastAPI
- Whether it can plausibly run on OCI E6 AX without GPU
- How it could pair with the vLLM companion layer
- Any licensing or production-use caveats to check

Please include options across different categories, such as:
- Tabular prediction / classification
- Anomaly detection
- Time-series forecasting
- Text classification
- Sentiment or intent classification
- Embeddings / similarity
- Computer vision, if practical
- Recommendation or ranking, if practical

After presenting the options, stop and ask me to choose one. Do not create the full project scaffold until I choose the model.

Phase 2: Repository structure — only after I pick a model
After I choose a model, create a clean repo layout for:
- `ml_service/` — serves the selected pre-existing public ML/statistical model
- `llm_service/` — wraps calls to a vLLM OpenAI-compatible endpoint
- `orchestrator_api/` — coordinates calls between the ML service, vLLM service, and database
- `db/` — schema, views, and sample seed data for Autonomous Database
- `docker/` or service-level Dockerfiles
- `deploy/` — OCI deployment notes, scripts, or manifests
- `tests/`
- `docs/`

Phase 3: ML service
Create a FastAPI-based ML service that:
- Loads the selected pre-trained public model artifact from its public source
- Does not perform model training
- Accepts the appropriate JSON input for the selected model
- Returns:
  - prediction score or model-specific output
  - normalized confidence/risk/label fields where appropriate
  - top drivers, explanations, or proxy drivers where available or approximated for the prototype
  - model name/source
  - model version or revision, if available
  - inference timestamp
- Includes a simple fallback/mock model if the public model cannot be loaded
- Is easy to replace later with a different public model artifact

Phase 4: vLLM companion layer
Create a client module that calls a vLLM OpenAI-compatible endpoint.
It should support:
- Generating a concise explanation from the selected model’s inference output
- Generating recommended next actions
- Optionally extracting structured features from free-text notes
- Configurable base URL, model name, timeout, and API key placeholder through environment variables

Phase 5: Orchestrator API
Create a FastAPI orchestrator that:
- Accepts one prediction request
- Calls the ML service
- Calls the vLLM companion client to generate an explanation and recommendation
- Returns a combined response
- Optionally writes the request and output to the database if DB settings are configured
- Handles graceful fallback if the vLLM endpoint is unavailable

Phase 6: Database layer
Create initial Oracle-compatible SQL for tables/views that could support:
- inference run logging
- input entity metadata
- selected model outputs
- normalized scores, labels, confidence values, or risk bands where appropriate
- driver contributions or proxy explanation fields
- LLM-generated explanations
- recommendations
- latest successful inference view

Phase 7: Docker and local development
Provide:
- Dockerfiles for each service where appropriate
- A docker-compose file for local prototype execution
- `.env.example`
- README instructions for running locally
- Health check endpoints

Phase 8: Production path notes for OCI
Create documentation covering:
- How this could run on OCI Compute using an E6 AX shape
- Whether vLLM is suitable on that shape, especially if GPU availability is relevant
- When to choose OCI Data Science Model Deployment instead of a custom FastAPI ML service
- Networking/security considerations
- Secrets handling
- Logging and monitoring
- Scaling considerations
- ADB connectivity
- Future APEX/OAC integration path

Phase 9: Testing
Add basic tests for:
- ML service prediction response shape
- selected model loading fallback behavior
- normalized output mapping
- vLLM client fallback behavior
- orchestrator combined response
- database write function mocked or optional

Phase 10: Deliverables
After I choose the model, generate the initial code scaffold, including:
- Python source files
- FastAPI apps
- Pydantic models
- config modules
- selected public model loading logic
- Dockerfiles
- docker-compose file
- SQL DDL
- README
- OCI deployment notes
- pytest tests

Use clear, maintainable Python. Prefer simple working code over complex abstractions. Include comments where future production implementation is expected.

Before writing code, briefly explain the architecture and assumptions, especially:
- vLLM is appropriate for LLM/embedding inference but not classical ML serving
- the statistical / ML model is pre-existing and should not be trained in this project
- Hugging Face is only one example source, not a hard requirement
- the project should begin by giving me model options and waiting for my selection
